'use client'
import { Element } from "react-scroll";

export default function Landing() {

    return (
        <Element name="landing" className={`border border-red-500 py-2 row-auto flex-grow py-80`}>Landing</Element>
    );
}