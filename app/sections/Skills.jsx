'use client'
import { Element } from "react-scroll";

export default function Skills() {
    return (
        <Element name="skills" className={`border border-red-500 py-2 flex-grow py-80`}>Skills</Element>
    );
}