'use client'
import { Element } from "react-scroll";

export default function About() {
    return (
        <Element name="about" className={`border border-red-500 py-2 flex-grow py-80`}>About</Element>
    );
}