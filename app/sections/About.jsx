'use client'
import { Element } from "react-scroll";

export default function About() {
    return (
        <Element name="about" className={`border border-red-500 flex-grow`}>About</Element>
    );
}