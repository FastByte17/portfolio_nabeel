'use client'
import { Element } from "react-scroll";

export default function Projects() {
    return (
        <Element name="projects" className={`border border-red-500 py-2 flex-grow py-80`}>Projects</Element>
    );
}