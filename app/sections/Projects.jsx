'use client'
import { Element } from "react-scroll";

export default function Projects() {
    return (
        <Element name="projects" className={`border border-red-500 flex-grow`}>Projects</Element>
    );
}